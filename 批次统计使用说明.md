 批次分析总耗时统计使用说明

## 功能说明

本功能用于追踪RabbitMQ异步分析任务的总耗时。当使用JMeter等工具进行批量测试时，可以统计从第一批请求提交到所有任务完成的总耗时。

## 使用方法

### 方法1：通过HTTP请求头传递批次信息（推荐）

在JMeter测试中，为每个请求添加以下HTTP请求头：

- `X-Batch-Id`: 批次ID（所有请求使用相同的批次ID）
- `X-Batch-Total`: 预期任务总数（例如：100）

**示例（JMeter配置）：**
1. 在HTTP请求的"HTTP信息头管理器"中添加：
   - 名称: `X-Batch-Id`，值: `jmeter_test_20240101_001`
   - 名称: `X-Batch-Total`，值: `100`

2. 所有100个请求都使用相同的批次ID和总数

### 方法2：在请求体中包含批次ID

在`SubmitAnswerBO`中添加`batchId`字段（可选）：

```json
{
  "candidateId": "xxx",
  "jobId": "xxx",
  "questionAnswerList": [...],
  "totalSeconds": 1200,
  "batchId": "jmeter_test_20240101_001"
}
```

**注意**：使用方法2时，需要先调用初始化接口或确保批次已初始化。

## 查询批次统计

### API接口

```
GET /interviewRecord/batchStats?batchId={批次ID}
```

**响应示例：**
```json
{
  "status": 200,
  "msg": "OK",
  "data": {
    "batchId": "jmeter_test_20240101_001",
    "startTime": "2024-01-01T10:00:00Z",
    "endTime": "2024-01-01T10:15:30Z",
    "totalCount": 100,
    "completedCount": 100,
    "totalDurationSeconds": 930,
    "totalDurationMillis": 930000,
    "isCompleted": true
  }
}
```

## 日志输出

当所有任务完成时，会在日志中输出：

```
==========================================
【批次统计】批次 jmeter_test_20240101_001 所有任务已完成！
【批次统计】总任务数: 100
【批次统计】开始时间: 2024-01-01T10:00:00Z
【批次统计】结束时间: 2024-01-01T10:15:30Z
【批次统计】总耗时: 930 秒 (930000 毫秒)
==========================================
```

## 对于已有数据的分析

如果您已经用JMeter测试过，但没有使用批次统计功能，可以通过以下方式分析：

### 方法1：查看应用日志

在日志中搜索以下关键词：
- `【RabbitMQ消费者】接收到AI分析任务`
- `【RabbitMQ消费者】任务处理完毕`

找到第一个任务开始时间和最后一个任务完成时间，计算差值。

### 方法2：使用日志分析工具

可以使用grep、awk等工具从日志文件中提取时间信息：

```bash
# 提取所有任务开始时间
grep "【RabbitMQ消费者】接收到AI分析任务" application.log | awk '{print $1, $2}'

# 提取所有任务完成时间
grep "【RabbitMQ消费者】任务处理完毕" application.log | awk '{print $1, $2}'
```

### 方法3：使用日志分析API

调用日志分析接口：

```
GET /logAnalysis/analyze?logFilePath=/path/to/your/application.log
```

**注意**：需要提供日志文件的绝对路径。

### 方法4：从数据库分析

如果任务结果已保存到数据库，可以通过`InterviewRecord`表的`create_time`字段分析：
- 最早记录时间 = 第一个任务开始时间
- 最晚记录时间 = 最后一个任务完成时间

## 注意事项

1. 批次数据在Redis中保留1小时，过期后需要重新初始化
2. 批次ID在同一时间段内应该唯一，建议使用时间戳或UUID
3. 如果任务失败进入DLQ，不会计入完成数
4. 幂等性检查：如果任务已处理过，不会重复统计

## 示例：JMeter测试配置

1. **线程组配置**：
   - 线程数：100
   - Ramp-up时间：10秒
   - 循环次数：1

2. **HTTP请求配置**：
   - 方法：POST
   - URL：`http://your-server/interviewRecord/collect`
   - Body：JSON格式的`SubmitAnswerBO`

3. **HTTP信息头管理器**：
   - `X-Batch-Id`: `jmeter_test_${__time(yyyyMMdd_HHmmss)}`
   - `X-Batch-Total`: `100`

4. **测试完成后查询统计**：
   - 使用`${__time(yyyyMMdd_HHmmss)}`生成的批次ID查询统计信息

## 快速开始

### 1. 在JMeter中添加请求头

在HTTP请求的"HTTP信息头管理器"中添加：
- `X-Batch-Id`: `test_100_users`
- `X-Batch-Total`: `100`

### 2. 运行测试

执行JMeter测试，发送100个请求。

### 3. 查询统计结果

测试完成后，调用：
```
GET http://your-server/interviewRecord/batchStats?batchId=test_100_users
```

或者查看应用日志，当所有任务完成时会自动输出总耗时统计。

